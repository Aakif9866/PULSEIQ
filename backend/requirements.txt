fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
email-validator==2.2.0

# Analytics engine (phase 3+)
duckdb==1.1.3
polars==1.17.1
openpyxl==3.1.5

# SQL validation for Natural Language to SQL (V2 phase 1) — real parsing,
# not a regex/keyword blocklist. See app/analytics/sql_validator.py.
sqlglot==26.33.0
# Required by duckdb.register() to accept a Polars DataFrame directly
# (Arrow-based zero-copy registration) — see app/analytics/sql_engine.py.
pyarrow==25.0.1

# Object storage (phase 2+)
boto3==1.35.90

# AI provider (phase 4+)
groq==0.13.1
httpx==0.28.1

# Structured logging
structlog==24.4.0

# Optional tracing (Phase 8 step 5) — inert unless OTEL_EXPORTER_OTLP_ENDPOINT
# is set; see app/core/tracing.py. Vendor-neutral OTLP, so it exports to
# Jaeger, Grafana Tempo, Honeycomb, or LangSmith's OTLP endpoint alike.
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
